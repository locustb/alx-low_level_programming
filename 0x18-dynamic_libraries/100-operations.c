#include <stdio.h>

int add(int a, int b) {
	return a + b;
}

int mul(int a, int b) {
	if (b == o) {
		printf ("Error: Division by zero\n");
		return o;

	}
	{	return a / b;

	}

	int mod(int a, int b){
		if (b == o) {
			prinf(Error: Division by zero\n");
			return o;
			)
				return a % b;
